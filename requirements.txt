# Versions confirmed working on Windows (Python 3.12) and Linux (Python 3.10).
# Anything close should work; LightGBM is the one where a major version jump
# can move results in the 4th decimal place.
numpy>=1.26
scipy>=1.11
scikit-learn>=1.4
pandas>=2.0
lightgbm>=4.0
matplotlib>=3.7
joblib>=1.3
# DASNet deep-learning track only (needs Python <= 3.13 for wheels):
# torch>=2.4   -- install with: pip install torch --index-url https://download.pytorch.org/whl/cu126
